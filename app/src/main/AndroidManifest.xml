<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pill_tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pill_Tracker">
        <activity android:name=".home2"/>
        <activity android:name=".mr_pillar_send_message" />
        <activity android:name=".mr_pillar" />
        <activity android:name=".edit_text_medicine" />
        <activity android:name=".edit_window" />
        <activity android:name=".add_dosage" />
        <activity android:name=".add_purpose" />
        <activity android:name=".add_schedule" />
        <activity android:name=".edit_window_dummy" />
        <activity android:name=".faq" />
        <activity android:name=".settings" />
        <activity android:name=".notification" />
        <activity android:name=".add" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".alert_receiver" />
    </application>

</manifest>